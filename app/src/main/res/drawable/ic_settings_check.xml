<?xml version="1.0" encoding="utf-8"?>
<!--
  `Icon / check` on the selected appearance row, node 2517:2825 / 2517:3792.

  The frozen glyph is 14 wide and 11 tall at x=5,y=6.5 inside a 24x24 slot,
  stroked 1.8 with round caps and joins - the same stroke contract every other
  line icon in this batch uses. The path is drawn to those bounds exactly:
  x runs 5..19 (14) and y runs 6.5..17.5 (11).

  `primary` rather than a token of its own: the frame fills it #1C4771 in Light and
  #5FD9B4 in Dark, which are that role's two values unchanged. The 2dp border on
  the same row is the same colour for the same reason - one selection accent, not
  two.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M5,12.5L9.5,17.5L19,6.5"
        android:strokeColor="@color/primary"
        android:strokeWidth="1.8"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
</vector>

<?xml version="1.0" encoding="utf-8"?>
<!--
  PLAYER play/pause surface.

  Frozen canonical: `Player Section > Controls > play/pause`, 80x80 with corner
  radius 20, filled with the `primary` role - #5FD9B4 mint in dark, #1C4771 navy
  in light, which is exactly what the role resolves to. One control for all three
  streams: the frozen design tints it by role, not by station, so the six
  per-stream drawables it replaces have no canonical counterpart.

  A ripple over the fill, masked to the same 20dp rounding, because this is the
  screen's primary control and the platform expects a press to show.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="@dimen/player_play_corner" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/primary" />
            <corners android:radius="@dimen/player_play_corner" />
        </shape>
    </item>
</ripple>

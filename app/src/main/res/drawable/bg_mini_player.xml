<?xml version="1.0" encoding="utf-8"?>
<!--
  Mini Player pill: the frozen surface plus a press ripple.

  The body opens PLAYER, so it is a control and the platform expects it to
  respond to a press. The frozen design defines no press state - it defines no
  interaction at all - so this adds the platform's own affordance and nothing
  else: `?attr/colorControlHighlight`, masked to the pill's own 16dp rounded
  rectangle so the ripple cannot paint over the corners the design does define.

  Geometry is untouched. The fill, the stroke and the radius all still come from
  bg_mini_player_surface, which is the drawable this used to be.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="@dimen/mini_player_corner" />
        </shape>
    </item>

    <item android:drawable="@drawable/bg_mini_player_surface" />
</ripple>

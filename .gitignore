*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing — never commit (see docs/RELEASE_SIGNING.md)
keystore.properties

# Supabase client config. Holds only public values (project URL + publishable
# key), but stays local so a fork does not inherit someone else's project.
# supabase.properties.example is the template. A SECRET key must never go in it.
supabase.properties
*.jks
*.keystore

# Release artifacts
*.apk
*.aab
/app/release/
/releases/

# Build outputs
/app/build/

# Build / signing / device logs
build_log*.txt
build_output*.txt
signed_bundle_log*.txt
*.logcat

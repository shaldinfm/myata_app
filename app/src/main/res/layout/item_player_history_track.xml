<?xml version="1.0" encoding="utf-8"?>
<!--
  PLAYER Broadcast History row, frozen 3.6.6 canonical (Phase C).

  Source: `Broadcast History Section > History Item N` - light nodes 2396:30794,
  2399:31072, 2399:31091; dark 2444:18298, 2444:18316, 2444:18334. All six are
  the same row and differ only in their fills.

  The frozen row is 324x74 with cornerRadius 8 and a 1px INSIDE stroke, and it
  paints neither: it has no fill and its stroke is #000000 at opacity 0. So there
  is no card here, only 13 of padding around its content.

     0    13         55           67               119                 311
     |    | time     |  gap 12    | 40x40 r6 art   | gap 12  title     |
          | 42 START |            |                |         artist    |

  **There are no service actions in this row.** The owner-confirmed FINAL PLAYER
  reference is time -> artwork -> title/artist and nothing else. Spotify, Apple
  Music and Yandex Music stay on the surfaces that do carry them - the History
  bottom sheet (`item_history_track.xml`) and Collection
  (`item_favorite_track.xml`), both unchanged - and `MusicSearchHelper` is
  untouched. Nothing is left behind here in their place: no hidden views, no
  reserved width beside the text, no reserved line beneath it.

  **Height.** 74 is 13 + 48 + 13, and the 48 is one line of the title's 28 over
  one line of the artist's 20. Every vertical number here is a MINIMUM: the row
  grows when a track name needs a second line and shrinks back when it does not.
  With the service line gone, one-line content measures the frozen 74 again, and
  three rows and a button make the frozen 374.

  The title takes at most two lines and ellipsizes at the end after that, by owner
  decision. That is deliberately NOT what item_history_track.xml does: the History
  bottom sheet keeps the older "add a line rather than lose the end" rule, and it
  is unchanged. The two surfaces differ on purpose - the inline section on PLAYER
  holds three rows inside a fixed card, so an unbounded title pushes the rest of
  the card down. The artist is not capped either way.

  **Alignment.** The text block is TOP-aligned to the artwork, not centred against
  it: the title's first line and the cover's top edge start on the same y, and the
  artist sits directly under the title. The artwork is pinned to the top of the
  content for the same reason.

  This replaces centring the artwork against the text block. Centring is stable
  only while the text is exactly one line taller than the cover, which is the one
  case the frozen mock draws; the moment a title wraps, the cover slides down by
  half the added line - 18 at two lines - and the title's first line no longer has
  anything to do with the top of the cover. The cost of top-aligning is that the
  cover and the time sit 4 higher than the frozen y=4 on a one-line row, where the
  mock centred a 40 cover in a 48 text block. The text itself does not move, and
  the row is still 74.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/history_row"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/player_history_row_padding">

    <!--
      `Container > Container > NN:NN`. Onest Regular 14/20 on `text_secondary` -
      Broadcast History's timestamp is Onest by the typography contract, and
      #42474E / #B3C4D1 is exactly that role on both pages.

      START aligned, by owner correction: every timestamp begins on the same
      vertical line at 13, so the column reads as a column. The width stays fixed
      at `player_history_time_width` - see the dimen, which carries the
      measurement - so the gap to the artwork is fixed too and the artwork starts
      at the same x on every row. A right-aligned or self-sizing column gives up
      one of those two alignments; a fixed box with START gives up neither.
    -->
    <TextView
        android:id="@+id/tv_time"
        android:layout_width="@dimen/player_history_time_width"
        android:layout_height="wrap_content"
        android:gravity="start"
        android:maxLines="1"
        android:minHeight="@dimen/player_history_time_height"
        android:textAppearance="@style/TextAppearance.Myata.Onest.Regular.14_20"
        android:textColor="@color/text_secondary"
        app:layout_constraintBottom_toBottomOf="@id/artwork"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/artwork"
        tools:text="10:45" />

    <!--
      `Background` + its `Album Art` child: a 40x40 r6 plate with an image fill
      set to CROP over it. HistoryTrack has no artwork field, so the cover is
      resolved from the row's artist and track through ArtworkRepository - the
      app's single source of truth for artwork, and already the ViewModel's - and
      the plate is what shows until it arrives, or for good if nothing is found.

      Pinned to the top of the content, so the title's first line can align to its
      top edge. It had a bottom constraint to tv_artist, which centred it in the
      text block and slid it down as the title wrapped.
    -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/artwork"
        android:layout_width="@dimen/player_history_artwork_size"
        android:layout_height="@dimen/player_history_artwork_size"
        android:layout_marginStart="@dimen/player_history_row_gap"
        android:background="@drawable/bg_player_history_artwork"
        android:contentDescription="@string/player_history_artwork"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toEndOf="@id/tv_time"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Myata.PlayerHistoryArtwork" />

    <!--
      `Container > Container`: the title above the artist, no gap between them -
      the frozen boxes abut at 28 and 48. Onest for both, by the typography
      contract's Broadcast History entry; 17/28 on `text_primary` over 14/20 on
      `text_secondary`, each an exact role match on both pages.

      The column runs to the row's own edge: nothing is beside it.

      The frozen mock uppercases nothing - "CRYOGEN", "MUSE" and the rest are
      stored that way in the design - so no textAllCaps here. The History bottom
      sheet uppercases its artist in code; the frozen section gives no instruction
      to, so the inline row shows what the API returns.
    -->
    <TextView
        android:id="@+id/tv_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/player_history_row_gap"
        android:ellipsize="end"
        android:maxLines="2"
        android:minHeight="@dimen/player_history_title_height"
        android:textAppearance="@style/TextAppearance.Myata.Onest.Regular.17_28"
        android:textColor="@color/text_primary"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/artwork"
        app:layout_constraintTop_toTopOf="@id/artwork"
        tools:text="MEET ME IN LOVE" />

    <TextView
        android:id="@+id/tv_artist"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:minHeight="@dimen/player_history_artist_height"
        android:textAppearance="@style/TextAppearance.Myata.Onest.Regular.14_20"
        android:textColor="@color/text_secondary"
        app:layout_constraintEnd_toEndOf="@id/tv_title"
        app:layout_constraintStart_toStartOf="@id/tv_title"
        app:layout_constraintTop_toBottomOf="@id/tv_title"
        tools:text="BLOSSOMS" />
</androidx.constraintlayout.widget.ConstraintLayout>

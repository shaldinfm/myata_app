# Rasterised comparisons: device captures and the composites built from them,
# regenerated by re-running the harness. The SVGs are the artefact worth keeping -
# they are the frozen design itself, and they diff.
*.png

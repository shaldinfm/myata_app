<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <attribution android:tag="myata_radio" android:label="@string/app_name" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    
    <!-- Explicitly remove AD_ID permission added by Firebase - app doesn't use ads -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    
    <!-- TV Support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    
    <!-- Bluetooth is optional - app works without it -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application
        android:name=".MyataApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/lastfm_backup_rules"
        android:dataExtractionRules="@xml/lastfm_data_extraction_rules"
        tools:targetApi="31"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/tv_banner"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        
        <!-- Mobile Entry Point.

             android:theme is the launch theme, not the app theme: it is the only
             theme the system can read before any of our code runs, and it is what
             gives the tap an immediate acknowledgement. installSplashScreen()
             hands the activity straight back to AppTheme.

             android:screenOrientation="portrait" - the phone app does not rotate.

             HOME is a portrait composition: a 390dp-wide column whose rows scroll
             horizontally and whose stream cards carry their own 16/15/43 lead,
             gap and peek (see fragment_main.xml). It is responsive on the
             vertical axis - a short screen trades section gaps, never type or a
             touch target, for the height it is missing - and it has no landscape
             variant, so a rotated phone would present the portrait column with
             roughly a third of a viewport to scroll in. The owner's decision is
             therefore to fix the mobile app in portrait rather than design a
             second composition, and this declaration is that decision: the whole
             app, because MainActivity is the only launcher activity on the phone.

             It is the platform mechanism rather than a runtime call on purpose.
             setRequestedOrientation from code runs after the first frame, so the
             launch window itself could be composed landscape and then re-laid
             out, and it would need a call in every entry path - the launcher
             intent, ACTION_PLAY, a notification tap - to cover the app. The
             manifest attribute applies before the first frame, to all of them.

             ## What this does and does not guarantee

             It is honoured on the phones this app ships to. Android 16 (API 36)
             ignores an orientation restriction on a display whose smallest width
             is 600dp or more - a tablet - which is the platform's adaptive-layout
             rule rather than anything this app opts into; there is no mobile
             landscape or large-screen layout here to present instead, so nothing
             is lost by that and nothing needs building for it. `configChanges`
             below still lists `orientation`, so even on such a display a rotation
             re-lays out rather than restarting the activity and the playback
             session with it.

             TvMainActivity carries no orientation restriction and must keep
             carrying none: a television is a landscape display and an orientation
             request there would be wrong twice over. MobileOrientationLockTest
             pins both halves. -->
        <activity
            android:windowSoftInputMode="adjustResize"
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Myata.Splash"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.PLAY"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- TV Entry Point -->
        <activity
            android:name=".TvMainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/TvTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MediaPlayerService"
            android:attributionTags="myata_radio"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>
    </application>

</manifest>

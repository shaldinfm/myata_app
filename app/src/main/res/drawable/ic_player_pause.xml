<?xml version="1.0" encoding="utf-8"?>
<!--
  PLAYER pause glyph. Same derivation as ic_player_play.

    Material Symbols `pause` at 24dp, "M6 19h4V5H6v14zm8-14v14h4V5h-4z"

  Two disjoint rectangles wound the same way, so the default non-zero fill is
  correct and no android:fillType is needed.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="@dimen/player_play_icon"
    android:height="@dimen/player_play_icon"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M6,19 L10,19 L10,5 L6,5 Z M14,5 L14,19 L18,19 L18,5 Z" />
</vector>

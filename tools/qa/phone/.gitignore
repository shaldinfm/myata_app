# Capture output: ~11 MB of screenshots per run, regenerated by the harnesses.
# The harnesses and their pass/fail verdicts are the artefact worth keeping.
*.png
shell-before/
shell-after/
donation-entry/
nav-icons/

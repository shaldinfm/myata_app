# Screenshots and hierarchy dumps are regenerated by the capture script and can
# be large. The committed evidence is the metadata.json for each run plus the
# comparison result; PNGs are kept only when a run is being reviewed.
*.png

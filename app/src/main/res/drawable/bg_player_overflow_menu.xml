<?xml version="1.0" encoding="utf-8"?>
<!-- Frozen `Menu / Плеер` 2444:18763: r20 on `menuSurface`, 1px inside `menuOutline`. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/menu_surface" />
    <stroke
        android:width="@dimen/sheet_stroke"
        android:color="@color/menu_outline" />
    <corners android:radius="@dimen/player_overflow_menu_radius" />
</shape>
